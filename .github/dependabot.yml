# Workflow actions are pinned to commit SHAs; Dependabot proposes the bumps
# (as reviewable PRs) so the pins do not silently go stale.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
