# Reidey — خارطة الطريق (Roadmap)

> آخر تحديث: **2026-09-04** · يوثّق وين وصلنا، شو ناقص, الأشياء اللي بدها تصليح/مراجعة، والأشياء المطلوبة منك.
> **ملف حيّ** — يتحدّث مع كل دفعة شغل ويترفع على الجيت. للتفاصيل الهندسية والتشغيلية اقرأ [`HANDOFF.md`](./HANDOFF.md) (فيه سجل تغييرات مؤرّق + دليل عمل الإيجنت).

---

## 📊 الحالة العامة

**إنتاج · الـMVP مكتمل** &nbsp; `▰▰▰▰▰▰▰▰▰▱` **~95%**

**الجيو الذاتي (Nominatim + OSRM) فعّال على البرودكشن** ومتحقَّق (مسافات صحيحة، بلا rate limit، جاهز للمقياس). **WebSocket حيّ** (العروض + خريطة السواقين على قناة الشركة المعزولة). **التسليم المتعدد مثبت شغّال end-to-end.**

المتبقّي: **إصدار iOS** (DSA قيد المراجعة) · قرار معالجة السعر للمحطة المضافة أثناء الرحلة · **توسّع** (شاردنغ) عند الحاجة. التفاصيل بالأسفل وفي [HANDOFF.md](./HANDOFF.md).

---

## 📅 شغل اليوم (2026-09-04) — فواتير + سائقين أونلاين + مراقبة أخطاء + إصلاح الجيو

### ✅ اللي خلصناه (مدموج + منشور)
- **الفواتير: PDF لكل فاتورة + محرّر قالب منظّم** — ما كان في PDF ولا إيميل فاتورة أصلاً (الـ"فاتورة" = صف `SubscriptionPeriod`). بنينا: dompdf + `InvoiceSettings` (لوجو/ألوان/مُصدِر/بنك/ضريبة/نصوص) + رقم فاتورة تسلسلي `RE-YYYY-####` + قالب Blade ألماني (Rechnung، net/MwSt/gross، §19 Kleinunternehmer) + محرّر أدمن بمعاينة حية + **إرفاق PDF بإيميل** التفعيل/التجديد + زر تنزيل PDF بالتقارير. i18n de/en/ar.
- **السائقون المتصلون بلوحة الأدمن** — كرت حي (عدّاد + مين هم + الشركة + حالة: متاح/في الطريق/على رحلة) عبر `OverviewController` (`Driver::scopeOnline()`).
- **الجيو: لا تخمين للمسافة** — نفس العنوان الناقص كان يعطي مسافات متناقضة (877م/65م) و€/km سخيف (76€). الآن المسافة تُملأ **فقط من مصدر موثوق** (waypoints أوبر عند القبول، أو عنوان كامل exact)؛ غير هيك **تضل فاضية** بلا €/km. (تأكّد: الـ65م كانت داتا أوبر الحقيقية لرحلة قصيرة ملغاة، مش bug.)
- **أصوات إشعار مخصّصة + علامة ستوب موحّدة + poll أسرع** — صوت عادي/متعدد (native، بدو `eas build`) · علامة ستوب زرقا متماثلة بكل الأسطح · poll الديمون 3ث/6ث.
- **مراقبة أخطاء (Sentry) على 4 أسطح** — باك/فرونت/ديمون/تطبيق، كل واحد مشروعه (منطقة EU). الثلاثة السيرفر أسرار GitHub، التطبيق بالـeas.json.

### ⚠️ درس تشغيلي (تعلّمناه بالطريقة الصعبة)
- تعديل أسرار GitHub غيّر `DB_PASSWORD` بالغلط → الـCI بيعيد كتابة `.env` كل deploy → الباك فقد MySQL. **احفظ قيم الأسرار خارجياً** (GitHub بيخفيها). التعافي: `ALTER USER 'ridy'` لتطابق الـ.env، أو رجّع السرّ. **Sentry التقطها فوراً** (قيمته ظهرت).

### ❓ مطلوب منك
- **`eas build --profile production` + submit** — لتنزيل الأصوات (native) + كل شغل التطبيق للمتاجر. (APK تجريبي جاهز للاختبار.)
- **جرّب محرّر الفاتورة** (`/admin/invoice-template`) وعدّل التصميم/البيانات كما تحب.
- (اختياري لاحقاً) تنبيه انتهاء VPS · السعر الديناميكي للمحطة المضافة.

---

## 📅 شغل اليوم (2026-09-03) — multi-stop مثبت + إصلاح الإشعارات + تسريع الخريطة الحية

### ✅ اللي خلصناه (مدموج + منشور)
- **التسليم المتعدد (multi-stop) مثبت شغّال end-to-end** — عرض حي بمحطتين (Winston: Luisenstraße → Graudenzer → Alarichstraße) ظهر صح بالتطبيق (per-leg +6.9km/+2.0km، `stops_count=2`, `geo_source=uber`). **الاكتشاف المهم:** أوبر بيرمّز المحطة الوسطية `CHECKPOINT_TYPE_VIA` والنهائية فقط `DROPOFF` — النمط `PICKUP·VIA·DROPOFF` **مش** `DROPOFF`×2. لا حاجة لأي endpoint إضافي؛ للتدقيق دوّر على `%CHECKPOINT_TYPE_VIA%`.
- **إصلاح placeholders الإشعارات** — `proxy_expiring` (`{days}`) و`session_needs_relink` (`{company}`) كانوا يظهروا حرفياً لأن المُرسِل ما بيمرّر القيمة. فُحصت كل الأنواع؛ الباقي سليم، `company_banned` بلا مُرسِل حي. (57 تست خضر)
- **multi-stop بالموبايل: القائمة + الخريطة** — الصفوف (`OfferCard`/`RecentRow` + القائمة الكلية) بتبيّن شارة "Mehrere Stopps · N"، و"فتح الخريطة" بيمرّر المحطات الوسطية كـ`waypoints` بجوجل مابس (المسار الكامل مش استلام→تنزيل بس). **OTA — يحتاج `eas update`.**
- **تسريع الخريطة الحية** — poll الديمون التكيّفي 4ث→**3ث** (سائق engaged) و10ث→**6ث** (idle)، قريب من معدّل تحديث أوبر (~4-5ث) + انزلاق 700ms بالمتصفح (`ac41232`). **ليش مش سوكيت بس؟** السوكيت بيوصّل سيرفر→لوحة فوراً؛ العنق هو قفزة الإكستنشن/الديمون→أوبر (أوبر بلا ستريم مواقع، فلازم poll).

### ⏳ قرارات / متابعة
- **السيناريو الديناميكي** (راكب يضيف محطة أثناء الرحلة — الصيدلية←البيت) — الاكتشاف + الإشعار **شغّالين أصلاً** (الـpipeline بيلتقط `VIA` جديد وبيبعت `notifyMultiStop`)، بس **السعر معضلة**: أوبر ما بيبعتلنا الأجرة المحدّثة. محتاج قرار: **أ** (جمّد €/km على العرض الأصلي + اعرض الأرجل المضافة — موصى) / **ب** (اخفِ €/km + "محدّثة في أوبر") / **ج** (على المسافة الممتدة — مضلّل).
- **`driver_auth_401`** — `no_bearer` (نداءات بلا توكن؛ الكود الحالي بيسكّتها، اللوجات القديمة تختفي بعد نشر الباك) + حالة المالك (User 17 ينادي endpoint السائق) تنحل بـ`eas update`.

### ❓ مطلوب منك
- **`cd driver-app && eas update --branch production`** — ينزّل شارات + خريطة multi-stop للأجهزة، ويوقف حالة المالك بالـ401.
- **اختَر خيار السعر (أ/ب/ج)** لأبني معالجة السيناريو الديناميكي (بيلمس الـpipeline الجيوي المستقر — بستنى تأكيدك).

---

## 📅 شغل اليوم (2026-09-02) — هجرة Fleet Hub + تعافي الديمون + جيوكودنغ-قبل-الإشعار + تلميع التطبيق

### ✅ اللي خلصناه (مدموج على `main`)
- **هجرة Uber "Fleet Hub"** — أوبر غيّرت `supplier.uber.com` → `fleethub.uber.com` (الروستر/الحالات؛ مضيف العروض `vsdispatch.uber.com` ما تغيّر). هاجرنا كل مكان: الباك (`services.uber.supplier_base_url`)، الديمون، **الإكستنشن (v1.15.4)** (matches + fetches + `inject.js` يقبل `supplier|fleethub`)، وروابط اللوحة. العَرَض لو انكسرت المزامنة: العروض تصل لكن الخريطة فاضية وكل العروض "Not taken".
- **كاشف انكسار المزامنة `fleet:check-sync`** — أمر مجدول كل 5د يسجّل `fleet.sync_stale` للشركات النشِطة الي عندها سواقين مربوطين بلا `status_synced_at` طازج (12د) — بالضبط عطل الهجرة. ظاهر بصفحة صحّة النظام.
- **الديمون يتعافى تلقائياً بعد إعادة الربط** — كانت البصمة على **عدد** الكوكيز، فإعادة الربط (توكن جديد بنفس العدد) ما تتكشف → الستريم يعلق على كوكيز ميتة (RAMEN 404) لحد restart يدوي. صارت البصمة على **قيمة** الكوكيز (`jarFingerprint`) → إعادة الربط تعيد تشغيل الستريم لحاله. **ما عاد لازم restart يدوي.**
- **جيوكودنغ قبل الإشعار + تفاصيل المحطات** — `TripGeocoder::enrichForNotify()` يعمل جيوكودنغ مقيّد بزمن (~2.5ث) **قبل** الدفع فالإشعار يحمل المسافة/€ بدل ما يوصل فاضي. الرحلات متعددة المحطات تُحلّ per-leg (`labelStops()` + مسافة كل محطة) وتظهر بـ`stops[]` باللوحة والتطبيق. **✅ مثبت شغّال end-to-end** على عرض حي بمحطتين (id 5192، "Winston": Luisenstraße → Graudenzer Straße `+6.9km` → Alarichstraße `+2.0km`) — التطبيق عرض **"Mehrere Stopps · 2 Abgabestellen"** مطابق للمخزّن (`stops_count=2`، `geo_source=uber`). **تصحيح مهم:** المحطة الوسطية تجي بنوع **`CHECKPOINT_TYPE_VIA`** والنهائية فقط `DROPOFF` — يعني نمط multi-stop = `PICKUP · VIA[…] · DROPOFF`، **مش** `DROPOFF`×2. ما في حاجة لأي endpoint إضافي؛ لتدقيق رحلة متعددة دوّر على `%CHECKPOINT_TYPE_VIA%` بـ`dispatch_network_logs`.
- **تصريف الطابور عبر الـscheduler** — البرودكشن queue=database وما كان في `queue:work`، فالجوبات ما اشتغلت (السبب الجذري لضياع الجيوكودنغ). `routes/console.php` صار يجدول `queue:work --stop-when-empty` كل دقيقة + `offers:backfill-geo` كل 10د + `fleet:check-sync` كل 5د + heartbeat للـscheduler. (إصلاح انهيار الـscheduler من anonymous `vendor` volume قديم عبر `--renew-anon-volumes`.)
- **صحّة النظام (أدمن): بنية تحتية + طابور + لوجات** — `InfrastructureHealthService` (عمق الطابور + heartbeat الـscheduler + وصول Reverb/Nominatim/OSRM) + `QueueAdminController` (failed/retry/flush/clear) + `LogViewerController` (تذييل ومسح لوج الباك والفرونت) → تبويبات جديدة: **health · services · logs · shards**.
- **خط Manrope** بالواجهة (أرقام/لاتيني) مع بقاء العربي على **Tajawal** (إصلاح كسر الخط العربي) · **صوت العرض داخل التطبيق** عبر سوكِت Reverb والتطبيق مفتوح (OTA) · **توحيد حبّة التاريخ** بين الإحصائيات والعروض · تقليم توكنات FCM الميتة + تنظيف عرض اللوغ.
- **منتقي تاريخ محدد بالتطبيق** — صف "اختر تاريخاً" يفتح تقويم شهري داخلي (يوم/شهر/سنة، الأيام المستقبلية معطّلة). **JS/RN صافي، بلا native module → OTA.**
- **نوع هاتف السائق** — `device_tokens` صار يحمل `device_name` + `os_version` (عبر `expo-device`)؛ يظهر كسطر تحت شارة "On app" باللوحة (مثال **"Pixel 7 · Android 14"**). **OTA.**
- **استبعاد السواق الحرّ (free-agent)** — سائق انضم لشركة أخرى ما عاد يظهر "حرّ" بقائمة الأدمن (`OrphanDriverController` يستثني الي عندهم صف نشِط لنفس الـuuid).
- **استقرار الاستنتاج** — أزلنا `TimelineReconciler` نهائياً؛ **الاستنتاج من حالة السائق فقط** (القرار المتّفق عليه — لا يُعاد تجربة المصالحة الزمنية).

### ⏳ اللي ضايل / بدو متابعة
- **WebSocket/SSE للّوحة (البند #7)** — ✅ **صار حيّ (2026-09-02)**: اللوحة + العروض + خريطة السواقين تشترك بقناة `company.{tenantId}` المعزولة وتتحدّث لحظياً، والـpolling بقي fallback.
- **جلسة أوبر من الأدمن (البند #4)** — لسا الأعقد؛ حالياً المدير يربط عبر الإكستنشن والديمون يمسك الجلسة سيرفر-سايد.
- **بروكسي سكني لكل شركة** — ضروري ليشتغل الديمون 24/7 (أوبر يحجب الداتاسنتر).
- **iOS** — ناقص `GoogleService-Info.plist` + مفتاح APNs على Firebase.
- **الجيوكودنغ عند المقياس** — عند آلاف الشركات فعّل استضافة Nominatim/OSRM الذاتية (جاهزة، `docs/self-hosted-geo.md`).

### ❓ مطلوب منك
- **شغّل الـmigration** على السيرفر (عمودَي هاتف السائق) + **حدّث التطبيق OTA** ليصير الالتقاط فعّال — الطريقة بالأسفل بردّي.
- **نشر الإكستنشن v1.15.4** على متجر كروم (هجرة fleethub) لو لسا قيد المراجعة.
- خصّص/اشترِ بروكسيات وعيّنها للشركات.

---

## 📅 شغل اليوم (2026-08-17) — إطلاق الإشعارات + إحكام أمن دورة حياة الأسطول + الجيوكودر

### ✅ اللي خلصناه (مدموج + منشور على البرودكشن)
- **الإشعارات (FCM) صارت شغّالة end-to-end** — إصلاح النشر: الكود يقرأ `FCM_CREDENTIALS` (ملف service-account) بينما النشر كان يكتب `FCM_SERVER_KEY` الميت؛ صار النشر يكتب ملف الاعتماد من secret `FCM_CREDENTIALS_JSON`. تست push وصل الجهاز. + تسجيل `dispatch_offer.notified devices=N` لكل عرض للتشخيص.
- **أدوات تشخيص**: `dispatch:diagnose` (صحة سشن كل شركة + توزيع حالات العروض) + درِل `--tenant` (supplier_cookies + تطابق UUID + آخر تحديث حالة) · `push:doctor` (جاهزية الإرسال + التوكنات).
- **حساب أوبر واحد = شركة واحدة** — على 3 طبقات: حارس الالتقاط (`uber_org_already_linked`)، وتوجيه العروض بالسشن الفعّال، و**unique index بالDB** + migration نظّف السشنات المكرّرة الموجودة.
- **`fleet.connected` middleware** — كل نقاط الاستيعاب من المتصفح (روستر/حالات/مركبات/عروض/مقاييس) مرفوضة (409) إلا لشركة متّصلة فعلاً + فحص `org_mismatch`. أقفل ثغرة "تحديث من أوبر يسحب من أي حساب مفتوح".
- **فك الربط = حذف شامل** — سائقين/مركبات/عروض/أجهزة/مقاييس/سشن + تحرير البروكسي + مسح `uber_org_uuid` + منع الربط التلقائي (autolink block) لحد Connect صريح (`fleet-session/reconnect` يفكّه لأي نسخة إضافة).
- **self-heal للتكرارات** — مزامنة الروستر تدمج صفوف السائق المكرّرة (نفس uuid) وتحافظ على الدعوة المعلّقة، مقيّدة صراحةً بالـtenant.
- **impersonation** (تسجيل دخول كشركة من الأدمن) + بانر إيقاف · **زر حذف كل بيانات الشركة** (أدمن) + تأكيد.
- **الجيوكودر قابل للتوجيه** (`services.geo.*` / `NOMINATIM_URL`,`OSRM_URL`) + **stack استضافة ذاتية Nominatim+OSRM** خلف profile `geo` + `docs/self-hosted-geo.md` (للمقياس بدون حدود المعدّل).
- **إصلاح النشر**: `optimize:clear` (route+config) كل دبلوي — الحُرّاس الجديدة تطبّق فوراً.
- **اللاندينج التسويقي المحايد** + صفحة خصوصية · **تطبيق السائق**: تفاصيل العرض الكاملة (علامة الجودة €€ + الراكب + الأوقات)، RTL صحيح (إلغاء forceRTL)، splash-gate للخطوط/الأيقونات، أيقونة تطبيق أكبر · شكل الإشعار (المقاييس بالسطر الأول، بلا "-->").
- **مراجعات أمنية**: مراجعة تغييرات الجلسة (لا Critical/High) + مراجعة دورة حياة الأسطول كاملة (3 فجوات انسدّت) → **عزل الشركات محكم، 194 تست ناجح**.
- **أدلة نشر**: `docs/14-publish-extension.md` (الإضافة) + `docs/15-publish-mobile-app.md` (المتاجر).

### ⏳ اللي ضايل / بدو متابعة (أولوية)
- **بناء تطبيق جديد** — الأيقونات/Tajawal/RTL/تفاصيل العرض بالكود، بس محتاجة بناء (OTA غير موثوق بعد تبديل حساب EAS). `eas build -p android --profile preview`.
- **إشعارات iOS** — ناقص `GoogleService-Info.plist` + مفتاح APNs على Firebase (القسم D بدليل النشر).
- **الجيوكودر عند المقياس** — لسا Nominatim/OSRM العام (محدود ~1/ث). عند ألف شركة: فعّل الاستضافة الذاتية (جاهزة) + انقل enrich لطابور (حالياً متزامن على مسار الاستقبال).
- **WebSocket/SSE للّوحة** (لسا polling) · ربط سيارة↔سائق · RAMEN accept-events (بدل الاستنتاج).

### ❓ مطلوب منك
- **بناء APK جديد** ونشره (Play internal testing).
- **ربط #7 صح** (فك بياناتها → افتح أوبر واربط بحسابها) + سائقيها يسجّلوا للإشعارات.
- إعداد iOS (Apple Developer + APNs) لو بدك آيفون.

---

> آخر تحديث سابق: 2026-08-16 · يوثّق وين وصلنا، شو ناقص، الأشياء اللي بدها تصليح/مراجعة، والأشياء المطلوبة منك.
> الترتيب المتفق عليه للتنفيذ القادم: **(ب) تابات الأدمن → (ج) شركات التحصيل + جلسة أوبر من الأدمن → (أ) الديمون يسحب الحالات باستمرار**.

---

## 0) 🎯 قائمة العمل الحالية (مرتّبة — نشتغل من فوق لتحت)

| # | البند | الحالة | ملاحظات |
|---|-------|--------|---------|
| 1 | **إشعارات الأدمن والشركات** — ربط كل حدث/أكشن بإشعار صحيح للطرف الصح | ✅ خلص | `Notifier` يفرّق الأحداث لـbell + FCM ويب + إيميل، مع تفضيلات لكل مستخدم + برودكاست أدمن مصفوف. (شغل 2026-08-16) |
| 2 | **الفواتير + التقارير + تصدير Excel** | ✅ خلص | فواتير اشتراكات تلقائية + تقارير إيرادات/انتهاء + Excel. (PRs #92، #93) |
| 3 | **(ج) شركات التحصيل** — كيان + دفعات + تقارير | ✅ خلص | محصّل بلا ربط ثابت؛ الدفعة تحدد المحصّل. (PRs #82، #83) |
| 4 | **فتح جلسة أوبر للشركة من الأدمن** (حقن كوكيز) | ⏳ الأعقد | عبر إكستنشن/تبويب مُدار |
| 5 | **اسم الشركة من أوبر** | 🔒 محجوب | مش موجود بالـHTML؛ محتاج ناتج getDrivers top-level منك |
| 6 | **خريطة مواقع السواقين الحيّة** | ✅ خلص | تبيّن السواقين على رحلة (EN_ROUTE/ON_TRIP) بإحداثيات حقيقية + مسار. (PRs #87، #89) |
| 7 | **WebSocket/SSE للوحة الويب** (تحديث حيّ بدل polling) | ✅ خلص | اللوحة + العروض يشتركوا بقناة الشركة المعزولة (`company.{tenantId}`) ويتحدّثوا لحظياً؛ الـpolling بقي كـfallback. يحتاج بناء الفرونت مع `NEXT_PUBLIC_REVERB_KEY` (= `REVERB_APP_KEY`). |
| 8 | **تطبيق السائق + FCM** (إشعارات فورية) | ✅ خلص | Expo كامل (لوجين/تفعيل/رئيسية/عروض/تفاصيل+عدّاد/بروفايل) + FCM v1 OAuth2 + دعوات + وضع صاحب الأسطول. باقي تلميع بصري. (شغل 2026-08-16) |
| 9 | **تشديد OTP للبرودكشن** | ✅ خلص | البرودكشن ما بيقبل كود ثابت أبداً + حارس SMTP. (PR #91) |
| 10 | **الأداء/الاستجابة** — لا لاج بوصول العروض | ⏳ مستمر | dedup + إرسال متزامن + قياس الزمن · **+ جيوكودنغ-قبل-الإشعار مقيّد بزمن (~2.5ث) + تصريف الطابور بالـscheduler** (2026-09-02) |

> **خلص مؤخراً:** الاشتراكات/الحظر · البروكسيات · الأونلاين الحقيقي · **قبول العرض (EN_ROUTE + ديمون مستمر)** · تفاصيل الشركة بتابات · **شركات التحصيل** · **الخريطة الحية** · **فواتير الاشتراكات + التقارير** · **تشديد OTP** · **باقات الاشتراك + الريسيلر + سجل الأكواد** · إعادة تصميم (باص 1). (PRs #37–#93)

> **جديد (2026-08-12):** كيان **باقات الاشتراك** (Plan CRUD) · دور **Reseller/المحصّل-كبائع** يولّد أكواد على باقة · **توليد الأدمن صار على باقة** (دفع اختياري) · **سجل الأكواد** (كل كود ودورة حياته: قيد الانتظار/مُفعّل/منتهي) بفلاتر + تواريخ + Excel — للأدمن كامل وللريسيلر أكواده فقط · صلاحية الكود **10 دقائق** · إصلاح تسريب بيانات cross-tenant للمستخدم بلا tenant. (تفصيل قسم "شغل اليوم 2026-08-12")

> **جديد (2026-08-13):** **دارك مود كامل** بنظام tokens دلالي + زر تبديل (PRs #122, #124, #126) · **أون-بوردينغ الشركات** (بوب-أب typewriter مترجم أول دخول) (#123) · **الأكواد داخل صفحة الاشتراكات** (عمود كود ببادج + مودال تفاصيل) وحذف صفحة الأكواد المستقلة (#125) · **الكولكتر**: الإجماليات من الأكواد المدفوعة + حذف سجل الدفعات اليدوي + **إعادة توليد الكود المنتهي** (#127) · **داش بورد أدمن أرقى** (بطاقة فوترة/اشتراكات + شارت إيرادات + دونات الجلسات) (#128) · **البروكسي**: السعر المدفوع + مصدر الشراء (#129). (تفصيل قسم "شغل اليوم 2026-08-13")

> **جديد (2026-08-16) — دفعة ضخمة (تطبيق السائق + FCM + إشعارات شاملة):** كل التفاصيل بقسم "شغل اليوم 2026-08-16" تحت.

---

## 📅 شغل اليوم (2026-08-16) — تطبيق السائق + FCM + منظومة إشعارات + وضع صاحب الأسطول

### ✅ اللي خلصناه (مدموج على `main`)
- **تطبيق السائق (Expo SDK 52 + expo-router)** — كامل: لوجين، تفعيل الدعوة (تعيين كلمة مرور)، الرئيسية (حالة السائق + إحصائيات اليوم + العرض النشط + آخر العروض)، قائمة العروض (بحث/فلاتر/ترقيم)، تفاصيل العرض (**عدّاد تنازلي دائري SVG** + الحالة + المسار + المسافة/الجودة + Open in Uber/Maps)، البروفايل (تابين: إحصائيات + إعدادات مع تبديل لغة de/en/ar + RTL). ثيم لايت/دارك مطابق للّوحة.
- **FCM HTTP v1 (OAuth2 service-account)** — `FcmPushSender` بتوكن JWT→OAuth2 مكاش ~55د؛ رسائل high-priority data توقظ التطبيق المغلق؛ fallback `LogPushSender` بلا credentials. مشروع Firebase `reidey-225e8`.
- **هوية السائق** — موديل `Driver` authenticatable + حارس `driver` منفصل (Sanctum bearer)؛ توكن متعدد الأشكال (Driver أو User لوضع صاحب الأسطول).
- **دعوات السائقين** — دعوة بالإيميل مع **fallback لإيميل أوبر المخزّن** (يصير إيميل الدخول)؛ صلاحية **7 أيام** تنهندل بالحالات (بادج "دعوة منتهية")؛ إصلاح الفرونت اللي كان يفشل مع سائق عندو `uber_email` فقط.
- **منظومة إشعارات كاملة** — `Notifier` يفرّق كل حدث لـ**bell + FCM ويب + إيميل**، مع **تفضيلات لكل مستخدم** (opt-out)؛ **حذف الإشعارات**؛ **بيل دروبداون** (آخر 5)؛ **برودكاست أدمن** لكل المستخدمين عبر **queue** (`SendAdminBroadcast`)؛ إشعارات إيميل للأحداث المهمّة.
- **FCM ويب للّوحة** — Firebase web push (service worker + VAPID) للمدير/الأدمن.
- **وضع صاحب الأسطول** — المدير يدخل التطبيق بإيميله ويشوف **كل عروض سواقينه** مسمّاة باسم السائق (قراءة فقط)؛ `Driver\FleetController` (fleet/me,home,offers,stats).
- **ربط البروكسي على عدد السائقين** بدل عدد الشركات (`Proxy::usedCount()`).
- **إصلاح دورة حياة العرض** — الحافة 2→1 (رحلات متتالية ظهر/كمل) + إنهاء العروض العالقة بعد 100 دقيقة + جيوكودنغ عند الاستقبال (Nominatim/OSRM) للمسافة/الإحداثيات.
- **صفحات أدمن**: صحة النظام + تصدير Excel للمستخدمين + إرسال إشعار من قائمة المستخدمين.
- **ريسبونسيف** — إصلاحات عرض اللوحات على الموبايل.
- **بوابة اشتراك السائق** — `EnsureDriverTenantActive` يمنع سواقين الشركة المعلّقة (403).
- **توثيق** — `README.md` + `HANDOFF.md` (عشان مودل تاني يكمّل).
- **تلميع بصري للتطبيق (آخر جلسة)** — الشعار صار مونوجرام SVG line-art مفرّغ (بدل PNG مصمت)، **خط Tajawal** بكل الأوزان، تصغير الحقول/الأزرار، تفعيل Forgot-password، تفاعل اللغة فوري (تبويبات ما تعلق)، إخفاء المسافة/الجودة بلا بيانات، **مجموعة favicon عالية الجودة**.

### ⏳ اللي ضايل / بدو متابعة
- **تلميع بصري نهائي للتطبيق** — مطابقة دقيقة لكل شاشة مع `driver-app/screens/` (أحجام/أيقونات/تباعد) بعد rebuild الـAPK.
- **بناء APK** (`eas build -p android --profile preview`) لمعاينة كل التعديلات على جهاز.
- **WebSocket/SSE للّوحة** (البند 7) — لسا polling.
- **جلسة أوبر من الأدمن** (البند 4) — الأعقد، لسا.
- **الأداء** (البند 10) — مراقبة زمن وصول العروض مستمرة.
- **نشر**: على السيرفر تأكّد من `FCM_CREDENTIALS` + `FCM_PROJECT_ID` بملف `.env` + ملف service-account مرفوع، ثم `config:clear`.

### ❓ قرارات/محتاج منك
- **رفع service-account JSON** لمسار `FCM_CREDENTIALS` على البرودكشن (بدونه البوش يتسجّل بس).
- مراجعة سكرينشوتات التطبيق بعد الـAPK لضبط أي تفاصيل بصرية باقية.

---

## 📅 شغل اليوم (2026-08-13)

### ✅ اللي خلصناه (مدموج على `main`)
- **دارك مود كامل** — نظام ألوان دلالي (CSS tokens على `:root`/`.dark` + `@theme inline` بـTailwind v4) + `ThemeProvider` (يحفظ بالـlocalStorage، سكربت pre-paint يمنع الوميض) + زر تبديل بالتوب-بار. حوّلنا ~700 استخدام لون مثبّت لـtokens عبر ~50 ملف. (PR #122)
- **إصلاحات دارك** بعد التجربة: تاب أكتيف أبيض-على-أبيض (#124) · سيارة البريلودر + حقول التاريخ native (color-scheme) + الـtoasts تتبع الثيم (#122) · z-index البريلودر فوق عناصر الخريطة (#126) · تطابق ألوان ليجند الخريطة + إزاحتها عن الزوم.
- **أون-بوردينغ الشركات** — بوب-أب ترحيبي أول تسجيل دخول للمدراء، نص يظهر بتأثير **كتابة تدريجية (typewriter)**، يشرح كل الوظائف (اربط أوبر → العروض → السائقون → الخريطة → الاشتراك)، مترجم en/de/ar + RTL، يظهر مرّة وحدة وقابل للإعادة من زر "؟". (PR #123)
- **الأكواد داخل صفحة الاشتراكات** — جدول الفواتير صار يبيّن **الباقة + الكود** (بادج ملوّن حسب الحالة)، والضغط على الكود يفتح مودال تفاصيله (الحالة/الشركة/المحصّل/القيمة/التواريخ). حذفنا صفحة `/admin/codes` المستقلة. (PR #125)
- **الكولكتر أُعيد تصميمه** — إجمالي المحصّل صار من **الأكواد المدفوعة الي ولّدها** (كل كود ريسيلر = دفعة مدفوعة مقدّماً)، وحذفنا **سجل الدفعات اليدوي / Record payment**. الكود المنتهي دون استخدام إله زر **إعادة توليد** بصفحة الريسيلر. (PR #127)
- **داش بورد الأدمن الأرقى** — بطاقة **الفوترة والاشتراكات** (شارت إيرادات شهري + إحصائيات: هذا الشهر/مستحقّات/اشتراكات نشطة/قرب الانتهاء) + **دونات لحالة الجلسات** بدل الأعمدة + KPIs محدّثة (إيرادات). (PR #128)
- **البروكسي** — إضافة **السعر المدفوع + مصدر الشراء** (migration + فورم + عمود التكلفة) لتتبّع مصاريف مجمّع البروكسي. (PR #129)
- **تنظيف** — حذف كل البرانشات المدموجة (محلي + بعيد)؛ الريبو نظيف (`main` فقط).

### ⏳ اللي ضايل / بدو متابعة
- **نشر الإكستنشن على السوق + تسهيل التنزيل/التحديث** — دليل كامل بقسم 8 تحت.
- **دورة حياة عروض الرحلات** — الخطة جاهزة ومخبّاة (branch `feature/offer-lifecycle` + stash)؛ نكمّلها التالي.
- بنود قسم 0: إشعارات (#1) · جلسة أوبر من الأدمن (#4) · WebSocket/SSE (#7) · تطبيق السائق + FCM (#8).

### ❓ قرارات/محجوب عليك
- عيّنة Uber لـ**Driver Activity** (نشاط السائق) واسم الشركة من `getDrivers`.

---

## 8) 🧩 نشر إكستنشن Reidey على السوق + تسهيل التنزيل/التحديث

> الهدف: توزيع مُتحكّم للعملاء، تحديث تلقائي بلا تدخّل، وتجربة تثبيت بنقرة.

### 8.1 التحضير قبل النشر
- **manifest.json (MV3):** ارفع `version` عند كل إصدار، `name`/`description` واضحين، أيقونات (16/48/128)، **أقل صلاحيات ممكنة**، و`host_permissions` محصورة بأوبر فقط (`*://*.uber.com/*`, `*://supplier.uber.com/*`).
- **أصول المتجر:** أيقونة 128px، لقطات شاشة (1280×800 أو 640×400)، وصف مختصر + مفصّل.
- **سياسة خصوصية (URL إلزامي):** الإكستنشن يتعامل مع جلسة أوبر (بيانات حساسة) — لازم صفحة سياسة خصوصية واضحة تشرح شو بنلتقط ولوين بيروح.

### 8.2 حساب المطوّر والرفع
- سجّل بـ **Chrome Web Store Developer Dashboard** (رسوم لمرة واحدة **$5**). للنشر باسم الشركة استخدم حساب/مجموعة مؤسسية.
- زِب مجلد الإكستنشن (بدون ملفات مصدر زائدة) → **New item** → ارفع الـzip → عبّي الـlisting → اختر الرؤية → Submit.
- **المراجعة:** عادة ساعات–أيام؛ إكستنشنات تلمس مواقع طرف ثالث قد تُراجَع أدقّ.

### 8.3 الرؤية (مهم — الإكستنشن حسّاس)
- **Unlisted (موصى):** يشتغل بالرابط فقط ولا يظهر بالبحث — أنسب لتوزيع مُتحكّم للعملاء بدون جذب انتباه.
- **Public:** يظهر بالبحث للجميع.
- **Private (Workspace/Group):** لمستخدمي مؤسستك فقط.

### 8.4 التحديث التلقائي (سهل)
- Chrome **يحدّث تلقائياً** من السوق — المستخدم ما بيعمل شي.
- كل إصدار: **ارفع `version` في manifest.json** (مثلاً 1.14.0 → 1.15.0) وارفع zip جديد → المستخدمون ياخذوا التحديث خلال ساعات.
- بديل self-hosted (لو ما بدك السوق): `update_url` + ملف `updates.xml` مستضاف + `.crx` موقّع — أعقد؛ **السوق أبسط وأنصح فيه**.

### 8.5 تسهيل التنزيل (UX) — أشياء نبنيها
- **زر "أضف إلى Chrome"** برابط السوق مباشر من **صفحة الاتصالات + الأون-بوردينغ** — نقرة واحدة.
- **صفحة إعداد بعد التثبيت** تفتح تلقائياً أول تشغيل: سجّل دخول أوبر → اربط (نستفيد من فلو الربط الحالي).
- **كشف تلقائي إذا مثبّت** (عبر `externally_connectable` أو ping) وإظهار الحالة باللوحة بدل تخمين المستخدم.
- تثبيت الأيقونة (pin) + إشعار قصير عند الربط الناجح.

### 8.6 تسهيل التحديث (UX) — أشياء نبنيها
- **فحص إصدار من الباك-إند:** الإكستنشن يبعث نسخته مع كل ingest؛ الباك-إند يقارنها بأحدث نسخة مدعومة، واللوحة تعرض بانر **"حدّث الإكستنشن"** بنقرة تفتح السوق لو كانت قديمة/غير مدعومة.
- بما إن السوق يحدّث تلقائياً، البانر بس للحالات الحرجة (نسخة عالقة/قديمة جداً).
- سجل تغييرات مختصر داخل الإكستنشن عند التحديث (اختياري).

---

## 📅 شغل اليوم (2026-08-11)

### ✅ اللي خلصناه اليوم (مدموج على `main`)
- **مزوّد الإيميل قابل للاختيار** — الأدمن بختار **SMTP أو Resend** ويحط الـAPI key من الإعدادات؛ نفس التيمبلتات. (PR #67)
- **زر/فورم "أرسل إيميل تجريبي"** — مودال فيه خانة إيميل المستلم. (PR #69، #70)
- **بطاقات إحصائيات العروض** بصفحة العروض (المجموع/المأخوذ/نسبة القبول). (PR #66)
- **صفحة تفاصيل الشركة بتابات جانبية** (تفاصيل/اشتراك/مدراء/سواقين/عروض/مركبات) + تاب عروض بأيام قابلة للطي وترقيم. + **تابات البروفايل** (معلومات/كلمة مرور).
- **مقاومة فقدان العروض** — نفس العرض بيجي على قناتين RAMEN لحظياً → سباق INSERT كان يكسر الباتش كامل بـ500. صار: نلتقط الـduplicate كـتكرار عادي + الإشعار غير حرِج + كل عرض بلوبه try/catch. **هذا سبب "العروض الضايعة".** (PR #72)
- **إصلاح لوجو الإيميل** — Caddy كان يوجّه `/email/*` لـNext.js (404) → صار يخدمها من Laravel public. (PR #73)
- **الإكستنشن — سيناريو الدخول الجديد** (PR #74، v1.13.0):
  - الربط يفتح **`account.uber.com`** المألوفة (مش صفحة الديسباتش المخيفة).
  - الـorg uuid **يُكتشف بالخلفية** من ريدايركت `supplier` (`/orgs/<uuid>/`) بجلسة المدير الحقيقية → رسالة "تم الربط" → التبويب يسكّر.
  - **سحب البيانات صار بالخلفية** عبر `chrome.alarms` كل دقيقة (حالات السواقين + قبول العرض + تحديث الروستر دورياً) **بدون فتح أي صفحة** — من IP المتصفّح الحقيقي (الديمون IP محظور من `supplier`). **هذا سبب الحالات/القبول الغلط.**
- **الصفحات القليلة المحتوى تملأ العرض** — شلنا `max-w` عن البروفايل/الإعدادات/الإشعارات. (PR #75)

### ⏳ اللي ضايل / بدو متابعة
- **تأكيد ميداني** إن سحب الخلفية بالإكستنشن فعلاً عبّى القبول/الحالات الصح بعد التحديث (مراقبة لوجات + مقارنة بأرقام أوبر: عمر برادعي 13 رحلة/100%).
- بقية قائمة العمل بالأسفل (قسم 0): إشعارات، فواتير/تقارير/Excel، شركات التحصيل، جلسة أوبر من الأدمن، تشديد OTP، FCM/WebSocket، تطبيق السائق.

### ❓ محتاج قرارات منك
- **مابنغ الإشعارات (البند 1)** — أي حدث يروح لمين وبأي قناة (تفصيل 7.5).
- **تطبيق السائق + FCM** — إعداد Firebase/APNs/SMTP منك حسب خطة `snappy-mapping-yeti.md`.

---

## 📅 شغل اليوم — الدفعة المسائية (2026-08-11 ليلاً)

### ✅ اللي خلصناه (مدموج على `main`)
- **الإكستنشن — سلسلة إصلاحات الربط** (وصلت v1.14.0):
  - الربط يفتح **`supplier.uber.com`** (بوابة الأسطول) — الـorg بينقرأ من الرابط مباشرة؛ صفحة `account` ما بتكشف الـorg (مؤكّد من اللوجات). (PR #80)
  - **جرّة كوكيز `supplier` منفصلة** تُخزّن مشفّرة → الديمون يسحب الحالات/الروستر 24/7 عبر البروكسي بدون كروم. (PR #77)
  - إصلاح: الكابتشر كان يُهمَل لما الـorg = null. (PR #78) · التبويب يتسكّر **بس عند الربط** مش كل زيارة. (PR #85)
- **صفحة الاتصالات تقلب Active تلقائياً** بعد الربط (polling كل 3ث). (PR #81)
- **شركات التحصيل** — كامل: كيان + سجلّ دفعات + تقارير + تصدير CSV؛ لا ربط ثابت (الدفعة تحدد المحصّل). (PRs #82، #83)
- **الخريطة الحية** (Leaflet + OSM) — السواقين على رحلة كنقاط + مسار الاستلام/التنزيل، تحديث كل 12ث. التقاط الإحداثيات + الـwaypoints بوسّعنا طلب `GetDriverLiveLocation`. (PRs #87، #89)
- **دقّة القبول** — نكشف "أخذ العرض" عند **`EN_ROUTE`** (أبكر إشارة) مش بس `ON_TRIP`. (PR #86)
- **فواتير الاشتراكات + التقارير** — فاتورة تلقائية كل تفعيل/تجديد؛ تقارير إيرادات شهرية (من دفعات المحصّلين) + اشتراكات قربت تنتهي + كشف لكل شركة + تصدير Excel. (PRs #92، #93)
- **تشديد OTP للبرودكشن** — البرودكشن ما بيقبل كود ثابت أبداً + تنبيه critical لو `MAIL=log`. (PR #91)
- **UI**: مكوّن Select قابل للبحث (بديل select2) بكل النظام · تابات أجمل · إزالة زر "تعطيل الشركة" المكرّر · "السواقون"→"السائقين" · إعادة تصميم باص 1 (خلفية دافئة/كروت أنعم/أزرار stone) · إصلاح محاذاة RTL للبروكسيات. (PRs #84، #88، #90)

### ⏳ اللي ضايل / بدو متابعة
- **تأكيد ميداني** (بعد rebuild + إكستنشن 1.14.0): الديمون بيسحب 24/7؟ · الخريطة بتبيّن الرحلات؟ · القبول صار أدق؟
- البنود المتبقية (قسم 0): **إشعارات (#1)** · فتح جلسة أوبر من الأدمن (#4) · WebSocket/SSE (#7) · تطبيق السائق + FCM (#8).

### ❓ قرارات/اختيارية
- **Geocoding** لمطابقة القبول بالإحداثيات — أجّلناه (العرض فيه عناوين نصّية بس، بلا إحداثيات؛ تقريبي). نراقب الدقّة أول.
- **ربط سيارة↔سائق يدوي** عنا (مستقل عن أوبر) — اختياري.
- **إعادة تصميم باص 2** (جراي دافئ أعمق) — بعد مراجعتك لباص 1.

---

## 📅 شغل اليوم (2026-08-12) — باقات + ريسيلر + سجل الأكواد + تلميع UI

### ✅ اللي خلصناه
- **باقات الاشتراك (Plans)** — كيان + CRUD بالأدمن (اسم/سعر/مدة/تفعيل). السعر والمدة صاروا يجوا من الباقة بدل الإدخال اليدوي.
- **دور Reseller (بائع الأكواد)** — المحصّل يصير عنده لوجين ويولّد كود تفعيل لشركة على باقة. صلاحية `codes.generate`، وحارس ضد قراءة بيانات أي شركة.
  - **أكواد الريسيلر دائماً مدفوعة** — صاحب الفليت بروح للمحصّل، بطلب كود، وبدفع ثمنه مقدّماً؛ فالفاتورة تتسجّل مدفوعة والدفعة موسومة بالبائع (`sold_by_collector_id`).
- **توليد الأدمن صار على باقة** — بدل أيام/مبلغ يدوي: اختيار باقة + **حالة الدفع اختيارية** (الأدمن ممكن يعطي اشتراك مجاني). نفس السيناريو بتاب اشتراك الشركة.
- **كيان `subscription_codes` (سجل الأكواد ودورة حياتها)** — كل كود (من ريسيلر أو أدمن) بيتخزّن مع: الكود، الباقة، القيمة، حالة الدفع، البائع (المحصّل)، الشركة، ووقت الإنشاء/التفعيل/الانتهاء. الحالة **مشتقّة** (مش مخزّنة): `قيد الانتظار / مُفعّل / منتهي`.
  - عند التفعيل: يتقفل قيد السجل (`activated_at` + ربط بالـ`subscription_period` المتولّد).
- **سجل الأكواد بالواجهة**:
  - **صفحة أدمن `/admin/codes`** — كل الأكواد لكل الريسيلرز + المولّدة من الأدمن، مع فلاتر (حالة/تاريخ من-إلى) + ترقيم + **تصدير Excel** + عمود المحصّل.
  - **بصفحة الريسيلر** — جدول "أكوادي" الي ولّدها هو فقط، بنفس الفلاتر والترقيم (بلا عمود محصّل).
  - مكوّن `CodesLedger` واحد مشترك (DRY) بين الشاشتين.
- **صلاحية الكود = 10 دقائق** (كانت دقيقتين) — يعطي الفليت وقت يدخّله متأخّر شوي.
- 🔴 **إصلاح تسريب بيانات cross-tenant** — مستخدم بلا tenant (ريسيلر) كان يشوف سواقين وعروض كل الشركات لأن الـglobal scope كان no-op. صار `ResolveTenant` يرفض 403 لأي مستخدم بلا tenant مش super_admin؛ ونقلنا `/me`+`/logout` لمجموعة بلا tenant-scope عشان لوجين الريسيلر يشتغل.
- **UI**:
  - **بار بحث موحّد** (`SearchInput`) بكل الصفحات — عرض ثابت مضغوط (`max-w-sm`) بدل ما يتمدّد على كل السطر، ونفس الستايل بكل مكان (الشركات/المستخدمين/العروض/الريسيلر).
  - **شلنا بادج "رحلات نشطة"** من الخريطة.
  - إدارة المستخدمين: حذف + كونفيرم، فلتر أدوار multi-select، تسمية "Fleet Manager".
  - البروكسيات: تاريخ انتهاء + عمود ملاحظات. · شكل سيارة (PNG شفّاف مدوّر للاتجاه) على الخريطة + قائمة سواقين جانبية. · تحويل الأرقام للاتيني دائماً + عناوين بالألماني.

### ⏳ اللي ضايل / بدو متابعة
- **نشر**: بعد الدمج لازم `migrate` + `db:seed --class=RolePermissionSeeder` بالبرودكشن (دور reseller + صلاحية codes.generate).
- **Driver Activity** (نشاط السائق التفصيلي) — محجوب على عيّنة Network من تبويب "Driver activity" بـ`supplier.uber.com` (Request URL/payload/response) منك.
- عدّاد "اليوم" بصفحة العروض يبيّن المجموع الحقيقي بدل عدد الصفحة المحمّلة — بانتظار موافقتك.

### ❓ قرارات منك
- أي عيّنة `GetDriverActivity/StatusHistory` لتفعيل نشاط السائق.

---

## 1) ✅ اللي خلص (مدموج على `main` — PRs #37–#53)

### الحساب والدخول والأمان
- **تسجيل ذاتي للشركات + OTP** — خانات منفصلة، رمز اختبار عبر `OTP_TEST_CODE`.
- **استعادة كلمة المرور بالـOTP** — 3 خطوات (إيميل → تحقق → كلمة جديدة + تأكيد).
- **رسائل الأخطاء مترجمة** (عربي/ألماني/إنجليزي) عبر أكواد ثابتة.
- **الشركة الجديدة تحتاج تفعيل** قبل الاستخدام (كود من الأدمن).
- **التفعيل من شاشة الدخول** ("عندك كود تفعيل؟") بدون تسجيل دخول.

### الاشتراكات والحظر (الأدمن)
- تعطيل/انتهاء الاشتراك → يمنع دخول المدير **والسواقين** → **شاشة تواصل دعم** (واتساب + إيميل قابلين للتعديل).
- **كود تفعيل** يولّده الأدمن (صالح دقيقتين) + **3 محاولات غلط = حظر**.
- فلاتر الشركات (الكل/مربوطة بأوبر/بروكسي/منتهية/محظورة) + **رقم جوال المالك** (اضغط اتصل).
- بطاقة **حالة الاشتراك** بلوحة الشركة (الأيام المتبقية + التفعيل + الانتهاء + شريط تقدّم).

### السواقين والعروض
- **الأونلاين**: التقطنا قيم أوبر الحقيقية `MONITORING_SUPPLY_STATUS_{ONLINE,ON_TRIP,OFFLINE}`؛ صلّحنا العدّاد؛ أضفنا **لوحة "السواقين أونلاين"** (بديل الخريطة، تتحدّث كل 45 ثانية).
- **تتبّع قبول العرض** (المسار أ): تحوّل السائق لـ`ON_TRIP` → آخر عرض إجاه يتسجّل "أخذ العرض"؛ بادج **أخذ/لم يأخذ**.
- صفحة العروض: شيلنا الحذف؛ أعمدة أنظف.

### الهوية والإصلاحات
- **الشعار الحقيقي** بكل مكان (سايدبار/لوجين/ريجيستر/بريلودر/فافيكون/الإيميل) — مُتّجه من ملف المصدر.
- 🔴 **إصلاح خطير**: زر "تعطيل الشركة" كان **يحذفها نهائياً** → صار تعطيل عكوس؛ الحذف النهائي صار واضح ومنفصل.
- مودال إعادة تعيين كلمة مرور بدل `window.prompt`.
- إصلاح السايدبار على الموبايل؛ رابط ربط أوبر → `supplier.uber.com`؛ إغلاق تبويب أوبر تلقائياً بعد الربط.

---

## 2) 🔜 القادم (بالترتيب المتفق عليه)

### (ب) تابات الأدمن — التالي مباشرة
- **تفاصيل الشركة على تابات**: تفاصيل / عروض / سواقين / مركبات.
  - يحتاج **endpoints أدمن جديدة** تجيب عروض/سواقين/مركبات شركة معيّنة (cross-tenant، مثل `CompanyController` الحالي).
- **صفحة الإعدادات على تابات** (SMTP / الدعم / البروكسي) بشكل أجمل.

### (ج) شركات التحصيل + جلسة أوبر من الأدمن
- **شركات التحصيل (ص)**: كيان جديد يضيفه الأدمن (اسم/جوال/عنوان). كل شركة (س) تُربط بمُحصّل (ص). تتبّع: مين سلّم وين ومتى وقديش المجموع.
  - يحتاج قرارات منتج (تحت في قسم 5).
- **فتح جلسة أوبر للشركة من لوحة الأدمن**: الأصعب تقنياً — حقن كوكيز الجلسة الملتقطة في متصفّح الأدمن (httpOnly ما بتنحقن بالـJS مباشرة؛ الحل الأرجح عبر الإكستنشن أو تبويب مُدار).

### (أ) الديمون يسحب الحالات باستمرار
- الديمون (ماسك جلسة أوبر 24/7) يسحب `GetDriverLiveLocation` كل ~10 ثواني ويكشف تحوّلات `ON_TRIP` **بدون ما حدا يفتح صفحة**.
- بيخلّي **الأونلاين وتتبّع القبول** دقيقين وطول اليوم (بدل الاعتماد على الإكستنشن وقت فتح الصفحة).

---

## 3) 🧰 أشياء بدها تصليح / مراجعة / تحسين

- **إسناد القبول تقريبي**: بنسجّل آخر عرض بالنافذة (5 دقايق)؛ لو رفض عرض وأخذ اللي بعده ممكن نخطئ. تحسين: الديمون أو أحداث تيار RAMEN (تحت).
- **التوقيت**: الأونلاين/القبول يتحدّثوا بس وقت مزامنة الإكستنشن (صفحة مفتوحة). → المسار (أ).
- **الخريطة الحيّة مستحيلة حالياً**: أوبر بيرجّع `lat/lng = 0,0` بهذا الـendpoint. لعمل خريطة حقيقية لازم نعكس هندسة **WebSocket** تبع صفحة "Live Map" عند أوبر (شغل بحثي منفصل).
- **شعار الإيميل بالإرسال الحقيقي** يعتمد على `APP_URL` صحيح (دومين عام). تأكد إنه مضبوط بالبرودكشن (Gmail بيشيل الـSVG وdata URI — لهيك بنستعمل PNG مستضاف).
- **ربط السائق بالمركبة**: الرد فيه `vehicleId` بس ما بنخزّن/نربط المركبة بالسائق. مطلوب لو بدنا "سيارة + رقم لوحة" أو تقارير أدق.
- **كروت إحصائيات العروض** (كم مقبول/غير مقبول) — مؤجّلة؛ صارت ممكنة بعد ما ضفنا `accepted`.
- **RAMEN accept-events**: نلتقط عيّنة `raw_payload` لعرض بعد قبوله/رفضه — لو فيه حالة قبول مباشرة، بتصير أدق من الاستنتاج وبتلغي الحاجة للـpolling.

---

## 4) 🧭 من الخطة الأصلية — لسا مؤجّل (كبير)

- **تطبيق السائق (React Native/Expo)** + **إشعارات فورية (FCM v1)** — من `snappy-mapping-yeti.md`. الأولوية: زمن وصول الإشعار للسائق (نافذة القبول ~5 ثواني).
  - Phase 1: إصلاح مسار الإشعار (OAuth2 لـFCM + إرسال متزامن inline).
  - Phase 2: دعوات السائقين بالإيميل.
  - Phase 3: تسجيل/دخول/ربط السائق + توكن الجهاز.
  - Phase 4: تطبيق Expo (شاشات العرض + العدّاد).

---

## 5) 🙋 مطلوب منك (Action items)

### نشر / تشغيل (بعد كل دمج — المشروع Docker)
```bash
cd /root/ridy
git pull origin main
docker compose -f docker-compose.prod.yml up -d --build frontend backend   # لا تبني بـdocker-compose.yml (بيثبّت localhost)
docker compose -f docker-compose.prod.yml exec backend php artisan migrate --force
docker compose -f docker-compose.prod.yml exec backend php artisan db:seed --class=RolePermissionSeeder --force   # دور reseller + codes.generate
```
- **`OTP_TEST_CODE=111111`** لازم يكون بملف **`/root/ridy/.env`** (مستوى الـcompose، مش `backend/.env`).

### إعدادات خارجية
- **SMTP** لإرسال الإيميلات الحقيقية (المزوّد + `MAIL_*` أو من إعدادات الأدمن). حالياً log/غير مضبوط.
- **بروكسي سكني لكل شركة** (للديمون — أوبر بيحجب سيرفرات الداتاسنتر).
- لاحقاً للتطبيق: **Firebase/FCM + APNs**.

### قرارات منتج (لشركات التحصيل — ج)
- حقول المُحصّل (ص): اسم/جوال/عنوان — تمام؟ في غيرها؟
- كيف نسجّل الدفعة؟ (مبلغ + تاريخ + ملاحظة يدوياً من الأدمن؟ أو تلقائي؟)
- شو التقارير المطلوبة؟ (مجموع لكل مُحصّل، كشف لكل شركة تحته…)

### التقاطات بيانات (تساعدني أبني أدق)
- **عيّنة `raw_payload`** لعرض **بعد** ما ينقبل/يترفض (نشوف إذا فيه حدث قبول → أدق من الاستنتاج).
- أي تغيّر بأسماء حقول أوبر لو لاحظته.

---

## 7) 🆕 إضافات مطلوبة (دفعة جديدة)

### 7.1 الإكستنشن — الربط عبر `account.uber.com` (⚠️ فيه عقبة تقنية)
- المطلوب: يفتح `account.uber.com` → تسجيل دخول → يزامن الجلسة → يسكّر التبويب مباشرة. وكل الروابط (عروض/سواقين/مركبات) تشتغل بدون فتح أي رابط تاني.
- **العقبة**: التقاط الجلسة عنا بيقرأ **org uuid** من الصفحة، و`account.uber.com` ما فيه org uuid. حالياً بنفتح `supplier.uber.com` (اللي بمرّرك تلقائياً على `account.uber.com` للدخول ويرجّعك) فنلتقط org uuid + نسكّر — يعني الفلو الحالي **أصلاً** بيعمل "افتح → دخول → التقاط → إغلاق" بدون ما تفتح الديسباتش.
- **الحل لاستخدام `account.uber.com`**: بعد التقاط الكوكيز، الإكستنشن يطلب **org uuid عبر API** (بدون فتح صفحة). محتاج منك **عيّنة**: طلب يرجّع قائمة org/الشركة (نلتقطه من الكونسول) — وبعدها بحوّل الربط لـ`account.uber.com` بالكامل.

### 7.2 إدارة البروكسيات (Proxy Pool) — نبنيه الآن
- إضافة بروكسيات عامة بالأدمن، كل بروكسي **بسعة** (عدد شركات) أحدّدها.
- **إسناد تلقائي**: عند إضافة شركة → تاخذ أول بروكسي متاح فيه سعة وتترتبط فيه.
- **تحرير الشاغر**: عند حذف الشركة، أو انتهاء اشتراكها، أو تعطيلها → يتحرّر شاغر بالبروكسي.
- **تنبيه** لما تقرب السعة تخلص.
- **القرار المطلوب**: "السعة" = أقصى عدد شركات لكل بروكسي (مؤكّد؟).

### 7.3 الفواتير + الاشتراكات + إدارة الشركات + تقارير/Excel
- فواتير للاشتراكات، تقارير قابلة للتصدير Excel، وتحسين إدارة الشركات.

### 7.4 الأداء والاستجابة (لا لاج بوصول العروض)
- ضمان وصول العروض فوراً بدون تأخير (المسار: extension/daemon → ingest → notify).
- مراجعة: dedup، الإرسال المتزامن، عدم وجود queue lag، قياس زمن الوصول.

---

### 7.8 أون-بوردينج للشركات الجديدة
- بعد أول تسجيل دخول: جولة/شرح للوحة (خطوات: اربط أوبر عبر الإكستنشن → شوف العروض → اربط السواقين).
- عرض العروض بشكل مرتّب وجذّاب + حالة الربط واضحة، وإرشاد لخطوة الربط لو لسا مش مربوط.

### 7.5 إشعارات الأدمن والشركات (ربط صحيح)
- مراجعة كل الأحداث اللي المفروض تطلّع إشعار (شركة جديدة سجّلت، جلسة أوبر انفصلت/رجعت، اشتراك قرب/انتهى، شركة محظورة، عرض جديد…) وربطها بإشعارات فعلية للأدمن وللشركة.
- التأكد إن الأكشنز (تفعيل/حظر/إنهاء اشتراك) بتولّد الإشعار الصح للطرف الصح.

### 7.6 تشديد الـOTP للبرودكشن (بعد ما نخلص)
- إزالة/تعطيل `OTP_TEST_CODE` عند الإطلاق الحقيقي (يرجع الكود عشوائي).
- التأكد إن SMTP شغّال والإيميلات توصل فعلياً.

### 7.7 قرار: FCM (تطبيق) مقابل WebSocket — الأسرع والأخف
**التوصية (تقسيم حسب الوجهة):**
- **جوال السائق (المسار الأهم — نافذة القبول ~5 ثواني):** **FCM high-priority data messages**. بيوصل حتى والتطبيق مغلق/بالخلفية (على مستوى النظام)، ما بيحتاج اتصال دائم، بيتحمّل الضغط ويسكيل، وأقل استهلاك بطارية. WebSocket بالجوال بيحتاج التطبيق مفتوح واتصال دائم — غير مناسب لإيقاظ تطبيق مغلق.
- **لوحة الويب (المدير يتابع العروض):** **WebSocket/SSE** لتحديث حيّ فوري وقت الصفحة مفتوحة (بديل الـpolling الحالي كل 5 ثواني).
- **الخلاصة:** FCM للتطبيق + WebSocket/SSE للوحة الويب. الاثنين مكمّلين، مش بديلين.
- الحمل/الأداء: الإرسال inline متزامن (Http::pool) بلا queue lag للـFCM (موثّق بخطة تطبيق السائق).

---

## 6) 📌 ملاحظات معمارية (مرجع سريع)
- **Backend**: Laravel (multi-tenant) — Controllers → Services → Repositories.
- **Frontend**: Next.js + i18n (en/de/ar، عربي RTL).
- **Daemon**: Node — ماسك تيار RAMEN 24/7 لكل شركة عبر بروكسي.
- **Extension** (MV3): بيلتقط جلسة أوبر + roster + الحالات من متصفّح المدير (أوبر بيحجب سيرفراتنا).
- **حالة أوبر الحقيقية**: `MONITORING_SUPPLY_STATUS_{ONLINE,ON_TRIP,OFFLINE}` · الموقع يرجع `0,0` (مموّه).
</content>
