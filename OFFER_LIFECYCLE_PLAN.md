<div dir="rtl" align="right">

# 🔄 خطة: إصلاح دورة حياة العروض (عوالق + بيانات ناقصة)

## 🎯 المشكلتان

1. **عروض عالقة "الرحلة جارية" للأبد** — رحلة "ساعة" مستحيلة.
2. **بيانات الرحلة ناقصة** — المسافة/سعر الكيلو = "—".

---

## 🔬 الأسباب الجذرية (من الكود)

### أ) العوالق — منطق الاستنتاج ناقص + هشّ
[DriverStatusIngestor](backend/app/Domain/Fleet/DriverStatusIngestor.php#L86) بيحرّك الحالة عبر **حوافّ** حالة السائق، وبيعالج 3 بس:
```
0→1 قبول   ·   1→2 بدء   ·   (1أو2)→0 إغلاق
```
**الحافة `2→1` (ON_TRIP → EN_ROUTE) مالها معالجة.** وهاي بالضبط اللي بتصير بالـ**back-to-back**:
السائق المشغول ما بيرجع `idle` بين الرحلات — بينتقل من رحلة (ON_TRIP) مباشرة لاستلام التالية (EN_ROUTE). فحافة الإغلاق `→idle` **ما بتصير**، والعرض **يعلق "started" للأبد**.

> الديمون أصلاً بيعمل poll كل **10 ثواني** ([config.js](dispatch-daemon/src/config.js#L45)) — فالمشكلة **مش السرعة**، المشكلة **منطق ناقص وهشّ** (أي poll ضايع = عالق دائم، ما في تصحيح ذاتي).

### ب) البيانات — geocode كسول بيُختم على الفشل
[TripGeocoder::enrich](backend/app/Domain/Dispatch/TripGeocoder.php#L27) بيشتغل **بس على أول فتح تفاصيل**، عبر Nominatim/OSRM المجانية (حد ~1 طلب/ثانية). وبيختم `geo_synced_at` **حتى لو فشل** → `enrich` بترجع فوراً بالمرات الجاية = **ما في إعادة محاولة أبداً**.

---

## 🛠️ الحل

### الجزء 1 — استنتاج بالحالة (state-based) بدل الحوافّ الهشّة

نعيد كتابة `applyTransition` لتصبح **مطابقة تقاربية (convergent)**: كل poll نسأل "شو المفروض يكون الوضع الآن؟" ونحاذيه — بدل "شو الحافة اللي صارت":

| حالة السائق الآن | عرضه النشط | الإجراء |
|---|---|---|
| `EN_ROUTE`/`ON_TRIP` | لا يوجد | اقبل آخر عرض معلّق (+ابدأ لو ON_TRIP) |
| `ON_TRIP` | `accepted` | ابدأه |
| `EN_ROUTE`/`ON_TRIP` | `started` **من رحلة سابقة** + في عرض أحدث معلّق | **اقفل القديم (completed)** واقبل الجديد ← *يحلّ الـback-to-back* |
| `idle`/offline | `started` | **اكتمل** |
| `idle`/offline | `accepted` (ما بدأ) | **ألغِ (canceled)** |

الميزة: حتى لو ضاع poll، **الـpoll الجاي بيصحّح تلقائياً** — ما في عوالق دائمة.

**قاعدة الـback-to-back (ملاحظتك):** عرض جديد والسائق `ON_TRIP` = **طبيعي**، ما نقفل الحالي. الإغلاق فقط لما يترك `ON_TRIP` **لتلك** الرحلة (→EN_ROUTE التالية أو →idle).

### الجزء 2 — شبكة أمان (finalizer)
أمر مجدول `offers:finalize-stale` (كل 5 دقايق):
- عرض `started` أقدم من **حد أقصى (60 دقيقة — قابل للنقاش)** → **اكتمل تلقائياً**.
- عرض `accepted` ما بدأ + أقدم من ~20 دقيقة والسائق مش engaged → **ألغِ**.
- يمسك أي حالة فلتت من الاستنتاج (سائق راح offline فجأة، فجوة poll طويلة).

### الجزء 3 — نافذة إسناد أطول للمشغولين
حالياً 10 دقايق ([OfferLifecycle::ATTRIBUTION_MINUTES](backend/app/Domain/Dispatch/OfferLifecycle.php#L19)). السائق المشغول بيقبل العرض التالي بعد ما يخلّص الحالي (ممكن >10 دقايق) → نمدّدها لـ**20 دقيقة** (أو نُسندها لحظة الحافة بغضّ النظر عن العمر لو السائق كان مشغول).

### الجزء 4 — إصلاح البيانات (geocode موثوق)
1. **ما نختم `geo_synced_at` إلا عند النجاح** — نضيف عدّاد `geo_attempts` ونعيد المحاولة (مع backoff) بدل ما نستسلم من أول فشل.
2. **geocode استباقي** عبر **queued job** عند وصول العرض (مش على فتح التفاصيل) — مع احترام حد Nominatim (طابور + تأخير)، فالبيانات جاهزة قبل ما المستخدم يفتح.
3. أمر `offers:backfill-geo` لإعادة معالجة العروض القديمة الناقصة.

---

## 🧪 التحقق (تستات)
- **Back-to-back:** `idle→EN_ROUTE→ON_TRIP→[عرض2]→EN_ROUTE→ON_TRIP→idle` ⇒ عرض1 **completed**، عرض2 **completed**، صفر عوالق.
- **poll ضايع:** نقفز `ON_TRIP` مباشرة لـ`idle` ⇒ completed.
- **finalizer:** عرض started عمره 90 دقيقة ⇒ يُقفل.
- **geocode:** فشل Nominatim ⇒ `geo_synced_at` يبقى null + retry ينجح لاحقاً.
- الجناح الحالي (139 تست) يظل أخضر.

---

## ❓ نقاط للنقاش قبل التنفيذ
1. **الحد الأقصى لمدة الرحلة** قبل الإغلاق التلقائي: **60 دقيقة** كافية؟ (أطول رحلة ألمانية معقولة ~90؟)
2. **نافذة الإسناد**: نمدّدها لـ20 دقيقة، ولا نُسند بالحافة بغضّ النظر عن العمر؟
3. **geocode**: queue استباقي (يحتاج queue worker شغّال بالحاوية) ولا نخليه كسول بس نصلّح الـretry؟
4. الـpoll 10ث تمام، ولا نخليه تكيّفي (أبطأ لما الكل idle) لتقليل الحمل؟

---

*ملف نقاش — التنفيذ بعد الاتفاق.*

</div>
